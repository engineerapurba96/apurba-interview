// Define the multiplier
const multiplier = 2;

// Loop from 1 to 10
for (let i = 1; i <= 10; i++) {
    // Calculate the result
    const result = multiplier * i;
    // Print the result
    console.log(`${multiplier} * ${i} = ${result}`);
}